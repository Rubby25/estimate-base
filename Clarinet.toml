[project]
name = "estimate-base"
authors = ["Stacks Developer"]
description = "A comprehensive value tracking and estimation contract"
telemetry = false
requirements = []
[contracts.estimate-base]
path = "contracts/estimate-base.clar"
depends_on = []

[repl]
costs_version = 2
parser_version = 2

[repl.analysis]
passes = ["check_checker"]

[repl.analysis.check_checker]
strict = false
trusted_sender = false
trusted_caller = false
callee_filter = false
